---
apiVersion: app/v1
kind: Deployment
metadata:
 name: flm
spec:
 replicas: 2
 selector:
  matchLabels: 
   app: swiggy
 templates:
  metadata:
   labels:
    app: swiggy
  spec:
   containers:
    - name: cont-1
      image: shaikmustafa/paytm:movie
      port:
       containerPort: 80
